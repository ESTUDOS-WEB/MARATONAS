Temos um arquivo JSON com o cadastro de varios cliente, consulte esse JSON.
Na consulta deve ser retornado os cliente que têm:
 * Idade entre 18 e 30 anos.
 * Que já tiveram um total de compras maior que 5.
 * Moram no estado de Minas Gerais (MG).

 Retorna os códigos desses clientes. 

OBS:
    use 'const listaClientes = require("./clientes.json");', para consulta o JSON.

Exemplo:
    Entrada: arquivo json
    Saída:
        Clientes: 
